---
title: Alert Dialog
slug: alert-dialog
radixUI: https://www.radix-ui.com/primitives/docs/components/alert-dialog
radixAPI: https://www.radix-ui.com/primitives/docs/components/alert-dialog#api-reference
---

# Alert Dialog

A modal dialog that interrupts the user with important content and expects a response.

<Flex style={{ gap: 8 }}>
  <RadixUI href="https://www.radix-ui.com/primitives/docs/components/alert-dialog" />
  <RadixAPI href="https://www.radix-ui.com/primitives/docs/components/alert-dialog#api-reference" />
</Flex>

## Installation

Install the component from your command line.

```bash
yarn add @rempi-ui/alert-dialog
```

## Example

<PreviewComponent name="alert-dialog" />

## API reference

### Root

Has common <strong>Props</strong> with [Radix Alert Dialog Root](https://www.radix-ui.com/primitives/docs/components/alert-dialog#root).

### Trigger

Has common <strong>Props</strong> with [Radix Alert Dialog Trigger](https://www.radix-ui.com/primitives/docs/components/alert-dialog#trigger).

<APIReference
  data={[
    {
      name: "asChild",
      required: false,
      type: "boolean",
      defaultValue: "true",
      description: (
        <>
          Change the default rendered element for the one passed as a child,
          merging their props and behavior.
          <br />
          <br />
          Read the <a href="https://www.radix-ui.com/primitives/docs/guides/composition">
            Radix Composition
          </a> guide for more details.
        </>
      ),
    },
  ]}
/>

### Portal

Has common <strong>Props</strong> with [Radix Alert Dialog Portal](https://www.radix-ui.com/primitives/docs/components/alert-dialog#portal).

### Overlay

A layer that covers the inert portion of the view when the dialog is open.<br/>
Has common <strong>Props</strong> with [Radix Alert Dialog Overlay](https://www.radix-ui.com/primitives/docs/components/alert-dialog#overlay).

<APIReference
  data={[
    {
      name: "blur",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: "Turns overlay background to blurry",
    },
  ]}
/>

### Content

Has common <strong>Props</strong> with [Radix Alert Dialog Content](https://www.radix-ui.com/primitives/docs/components/alert-dialog#content).

### Cancel

Has common <strong>Props</strong> with [Radix Alert Dialog Cancel](https://www.radix-ui.com/primitives/docs/components/alert-dialog#cancel).

<APIReference
  data={[
    {
      name: "asChild",
      required: false,
      type: "boolean",
      defaultValue: "true",
      description: (
        <>
          Change the default rendered element for the one passed as a child,
          merging their props and behavior.
          <br />
          <br />
          Read the <a href="https://www.radix-ui.com/primitives/docs/guides/composition">
            Radix Composition
          </a> guide for more details.
        </>
      ),
    },
  ]}
/>
### Action

Has common <strong>Props</strong> with [Radix Alert Dialog Action](https://www.radix-ui.com/primitives/docs/components/alert-dialog#action).

<APIReference
  data={[
    {
      name: "asChild",
      required: false,
      type: "boolean",
      defaultValue: "true",
      description: (
        <>
          Change the default rendered element for the one passed as a child,
          merging their props and behavior.
          <br />
          <br />
          Read the <a href="https://www.radix-ui.com/primitives/docs/guides/composition">
            Radix Composition
          </a> guide for more details.
        </>
      ),
    },
  ]}
/>

### Title

Has common <strong>Props</strong> with [Radix Alert Dialog Title](https://www.radix-ui.com/primitives/docs/components/alert-dialog#title).

### Description

Has common <strong>Props</strong> with [Radix Alert Dialog Description](https://www.radix-ui.com/primitives/docs/components/alert-dialog#description).

### Footer

A `<div/>` that in desktop resolution is aligned to the lower right corner and in mobile resolution is aligned to the center.

## Usage

Import the <strong>AlertDialog</strong> and piece all composition components together.

```jsx
import { AlertDialog } from "@rempi-ui/alert-dialog";
import { Button } from "@rempi-ui/button";

() => (
  <AlertDialog.Root>
    <AlertDialog.Trigger>
      <Button color="error">Delete account</Button>
    </AlertDialog.Trigger>
    <AlertDialog.Portal>
      <AlertDialog.Overlay />
      <AlertDialog.Content>
        <AlertDialog.Title>Are you absolutely sure?</AlertDialog.Title>
        <AlertDialog.Description>
          This action cannot be undone. This will permanently delete your
          account and remove your data from our servers.
        </AlertDialog.Description>
        <AlertDialog.Footer>
          <AlertDialog.Cancel>
            <Button variant="text">Cancel</Button>
          </AlertDialog.Cancel>
          <AlertDialog.Action>
            <Button color="error">Yes, delete account</Button>
          </AlertDialog.Action>
        </AlertDialog.Footer>
      </AlertDialog.Content>
    </AlertDialog.Portal>
  </AlertDialog.Root>
);
```

<NavigationNextPrevious
  previous={{
    link: "/docs/accordion",
    label: "Accordion",
  }}
  next={{
    link: "/docs/aspect-ratio",
    label: "Aspect ratio",
  }}
/>
