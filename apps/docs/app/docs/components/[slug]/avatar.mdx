---
title: Avatar
slug: avatar
radixUI: https://www.radix-ui.com/primitives/docs/components/avatar
radixAPI: https://www.radix-ui.com/primitives/docs/components/avatar#api-reference
---

# Avatar

<Description text="An image element with a fallback for representing the user." />

<Flex style={{ gap: 8 }}>
  <RadixUI href="https://www.radix-ui.com/primitives/docs/components/avatar" />
  <RadixAPI href="https://www.radix-ui.com/primitives/docs/components/avatar#api-reference" />
</Flex>

## Example

<PreviewComponent name="avatar" />

## Installation

Install the component from your command line.

```bash
yarn add @rempi-ui/avatar
```

## Usage

Import the <strong>Avatar</strong> and piece all composition components together.

```jsx
import { Avatar } from "@rempi-ui/avatar";

() => (
  <Avatar.Root size="standard">
    <Avatar.Image
      src="https://images.unsplash.com/photo-1492633423870-43d1cd2775eb?&w=128&h=128&dpr=2&q=80"
      alt="Colm Tuite"
    />
    <Avatar.Fallback delayMs={600}>CT</Avatar.Fallback>
  </Avatar.Root>
);
```

## About

Rempi-UI Avatar component is built on top of [Radix Avatar](https://www.radix-ui.com/primitives/docs/components/avatar).

## API reference

### Root

Contains all the parts of an avatar.

<MoreInfo
  componentName="Radix Avatar Root"
  componentLink="https://www.radix-ui.com/primitives/docs/components/avatar#root"
/>

### Image

The image to render. By default it will only render when it has loaded. You can use the `onLoadingStatusChange` handler if you need more control.

<MoreInfo
  componentName="Radix Avatar Image"
  componentLink="https://www.radix-ui.com/primitives/docs/components/avatar#image"
/>

<APIReference
  data={[
    {
      name: "onLoadingStatusChange",
      type: '(status: "idle" | "loading" | "loaded" | "error") => void',
      typeSimple: "function",
      description:
        "A callback providing information about the loading status of the image. This is useful in case you want to control more precisely what to render as the image is loading.",
    },
  ]}
/>

### Fallback

An element that renders when the image hasn't loaded. This means whilst it's loading, or if there was an error. If you notice a flash during loading, you can provide a `delayMs` prop to delay its rendering so it only renders for those with slower connections. For more control, use the `onLoadingStatusChange` handler on `Avatar.Image`.

<MoreInfo
  componentName="Radix Avatar Fallback"
  componentLink="https://www.radix-ui.com/primitives/docs/components/avatar#fallback"
/>

<APIReference
  data={[
    {
      name: "delayMs",
      type: "number",
      description:
        "Useful for delaying rendering so it only appears for those with slower connections.",
    },
  ]}
/>

<NavigationNextPrevious
  previous={{
    link: "/docs/components/aspect-ratio",
    label: "Aspect Ratio",
  }}
  next={{
    link: "/docs/components/badge",
    label: "Badge",
  }}
/>
