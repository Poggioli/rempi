---
title: Menu Bar
slug: menubar
radixUI: https://www.radix-ui.com/primitives/docs/components/menubar
radixAPI: https://www.radix-ui.com/primitives/docs/components/menubar#api-reference
---

# Menu Bar

<Description text="Um menu visualmente persistente comum em aplicativos de desktop que oferece acesso rápido a um conjunto consistente de comandos." />

<Flex style={{ gap: 8 }} alignItems="center">
  <RadixUI href="https://www.radix-ui.com/primitives/docs/components/menubar" />
  <RadixAPI href="https://www.radix-ui.com/primitives/docs/components/menubar#api-reference" />
  <SelectVersion />
</Flex>

<PreviewComponent name="menubar" />

## Instalação

Instale o componente a partir da linha de comando.

```bash
yarn add @rempi-ui/menubar
```

## Uso

Importe o <strong>MenuBar</strong> e junte todas as partes do componente.

```jsx
import { MenuBar } from "@rempi-ui/menubar";

const RADIO_ITEMS = ["Andy", "Benoît", "Luis"];

const CHECK_ITEMS = [
  "Sempre mostrar barra de favoritos",
  "Sempre mostrar URLs completas",
];

() => (
  <Menubar.Root>
    <Menubar.Menu>
      <Menubar.Trigger>Arquivos</Menubar.Trigger>
      <Menubar.Content align="start">
        <Menubar.Item>
          Nova aba <div>⌘ T</div>
        </Menubar.Item>
        <Menubar.Item>
          Nova janela <div>⌘ N</div>
        </Menubar.Item>
        <Menubar.Item disabled>Nova janela anônima</Menubar.Item>
        <Menubar.Separator />
        <Menubar.SubMenu>
          <Menubar.SubMenuTrigger>Compartilhar</Menubar.SubMenuTrigger>
          <Menubar.SubMenuContent alignOffset={-5}>
            <Menubar.Item>Email Link</Menubar.Item>
            <Menubar.Item>Mensagens</Menubar.Item>
            <Menubar.Item>Notas</Menubar.Item>
          </Menubar.SubMenuContent>
        </Menubar.SubMenu>
        <Menubar.Separator />
        <Menubar.Item>
          Imprimir... <div>⌘ P</div>
        </Menubar.Item>
      </Menubar.Content>
    </Menubar.Menu>

    <Menubar.Menu>
      <Menubar.Trigger>Editar</Menubar.Trigger>
      <Menubar.Content align="start">
        <Menubar.Item>
          Desfazer <div>⌘ Z</div>
        </Menubar.Item>
        <Menubar.Item>
          Refazer <div>⇧ ⌘ Z</div>
        </Menubar.Item>
        <Menubar.Separator />
        <Menubar.SubMenu>
          <Menubar.SubMenuTrigger>Buscar</Menubar.SubMenuTrigger>

          <Menubar.SubMenuContent alignOffset={-5}>
            <Menubar.Item>Buscar na internet...</Menubar.Item>
            <Menubar.Separator />
            <Menubar.Item>Buscar</Menubar.Item>
            <Menubar.Item>Buscar próximo</Menubar.Item>
            <Menubar.Item>Buscar anterior</Menubar.Item>
          </Menubar.SubMenuContent>
        </Menubar.SubMenu>
        <Menubar.Separator />
        <Menubar.Item>Cortar</Menubar.Item>
        <Menubar.Item>Copiar</Menubar.Item>
        <Menubar.Item>Colar</Menubar.Item>
      </Menubar.Content>
    </Menubar.Menu>

    <Menubar.Menu>
      <Menubar.Trigger>Visualizar</Menubar.Trigger>
      <Menubar.Content align="start" alignOffset={-14}>
        {CHECK_ITEMS.map((item) => (
          <Menubar.CheckboxItem
            key={item}
            checked={checkedSelection.includes(item)}
            onCheckedChange={() =>
              setCheckedSelection((current) =>
                current.includes(item)
                  ? current.filter((el) => el !== item)
                  : current.concat(item)
              )
            }
          >
            {item}
          </Menubar.CheckboxItem>
        ))}
        <Menubar.Separator />
        <Menubar.Item>
          Recarregar <div>⌘ R</div>
        </Menubar.Item>
        <Menubar.Item disabled>
          Recarregamento forçado <div>⇧ ⌘ R</div>
        </Menubar.Item>
        <Menubar.Separator />
        <Menubar.Item>Mudar para tela cheia</Menubar.Item>
        <Menubar.Separator />
        <Menubar.Item>Esconder barra lateral</Menubar.Item>
      </Menubar.Content>
    </Menubar.Menu>

    <Menubar.Menu>
      <Menubar.Trigger>Perfil</Menubar.Trigger>
      <Menubar.Content align="start" alignOffset={-14}>
        <Menubar.RadioGroup
          value={radioSelection}
          onValueChange={setRadioSelection}
        >
          {RADIO_ITEMS.map((item) => (
            <Menubar.RadioItem key={item} value={item}>
              {item}
            </Menubar.RadioItem>
          ))}
          <Menubar.Separator />
          <Menubar.Item>Editar</Menubar.Item>
          <Menubar.Separator />
          <Menubar.Item>Adicionar perfil</Menubar.Item>
        </Menubar.RadioGroup>
      </Menubar.Content>
    </Menubar.Menu>

    <Menubar.Menu>
      <Menubar.Trigger disabled>Empresas</Menubar.Trigger>
    </Menubar.Menu>
  </Menubar.Root>
);
```

## Sobre

<strong>Rempi-UI Menu Bar</strong> é baseado no [Radix Menu Bar](https://www.radix-ui.com/primitives/docs/components/menubar).

## API reference

### Root

Contém todas as partes do menubar.

<MoreInfo
  componentName="Radix Menu Bar Root"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#root"
/>

<APIReference
  data={[
    {
      name: "defaultValue",
      required: false,
      type: "string",
      description: (
        <span>
          O valor do menu que deve estar aberto quando inicialmente renderizado.
          Use quando você não precisa controlar o estado do valor.
        </span>
      ),
    },
    {
      name: "value",
      required: false,
      type: "string",
      description: (
        <span>
          O valor controlado do menu a ser aberto. Deve ser usado em conjunto
          com <code>onValueChange</code>.
        </span>
      ),
    },
    {
      name: "onValueChange",
      required: false,
      type: "(value: string) => void",
      typeSimple: "function",
      description: "Evento chamado quando o valor muda.",
    },
    {
      name: "dir",
      required: false,
      type: '"ltr" | "rtl"',
      typeSimple: "enum",
      description: (
        <span>
          A direção de leitura. Se omitido, herda globalmente de{" "}
          <code>DirectionProvider</code> ou assume o modo de leitura da esquerda
          para a direita (LTR, do inglês "left-to-right").
        </span>
      ),
    },
    {
      name: "loop",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Quando <code>true</code>, a navegação pelo teclado fará um loop do
          último item para o primeiro e vice-versa.
        </span>
      ),
    },
  ]}
/>

### Menu

Um item de menu de nível superior, que contém um gatilho com uma combinação de conteúdo.

<MoreInfo
  componentName="Radix Menu Bar Menu"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#menu"
/>

<APIReference
  data={[
    {
      name: "value",
      required: false,
      type: "string",
      description: (
        <span>
          Um valor único que associa o item a um valor ativo quando o menu de
          navegação é controlado. Essa propriedade é gerenciada automaticamente
          quando não é controlada.
        </span>
      ),
    },
  ]}
/>

### Trigger

O botão que alterna o conteúdo. Por padrão, o `Menubar.Content` se posicionará em relação ao gatilho.

<MoreInfo
  componentName="Radix Menu Bar Trigger"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#trigger"
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
    {
      name: "[data-highlighted]",
      value: "Presente quando destacado.",
    },
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### Content

É uma mistura de [Radix Menu Bar Portal](https://www.radix-ui.com/primitives/docs/components/menubar#portal) e [Radix Menu Bar Content](https://www.radix-ui.com/primitives/docs/components/menubar#content).<br/><br/>
Quando usado, "portais" a parte de conteúdo para o `body`. O componente que aparece quando um menu está aberto.

<MoreInfo
  componentName="Radix Menu Bar Portal"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#portal"
/>

<MoreInfo
  componentName="Radix Menu Bar Content"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#content"
/>

<APIReference
  data={[
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Usado para forçar a montagem quando mais controle é necessário. Útil
          ao controlar animações com bibliotecas de animação React. Se usado
          nesta parte, ele será herdado por <code>Menubar.Content</code> and{" "}
          <code>Menubar.SubContent</code> respectively.
        </span>
      ),
    },
    {
      name: "container",
      type: "HTMLElement",
      defaultValue: "document.body",
      description:
        "Especifique um elemento de contêiner para portar o conteúdo para dentro.",
    },
    {
      name: "loop",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Quando <code>true</code>, a navegação pelo teclado fará um loop do
          último item para o primeiro e vice-versa.
        </span>
      ),
    },
    {
      name: "onCloseAutoFocus",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o foco se move para o acionador após o
          fechamento. Pode ser impedido chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onEscapeKeyDown",
      type: "(event: KeyboardEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando a tecla <code>Esc</code> está pressionada. Pode
          ser evitado chamando <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onPointerDownOutside",
      type: "(event: PointerDownOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando ocorre um evento de ponteiro fora dos limites do
          componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onFocusOutside",
      type: "(event: FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o foco se move para fora dos limites do
          componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onInteractOutside",
      type: "(event: PointerDownOutsideEvent | FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando ocorre uma interação (evento de ponteiro ou
          foco) fora dos limites do componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Usado para forçar a montagem quando mais controle é necessário. Útil
          ao controlar animações com bibliotecas de animação React. Ele herda de
          <code>Menubar.Portal</code>.
        </span>
      ),
    },
    {
      name: "side",
      type: '"top" | "right" | "bottom" | "left"',
      typeSimple: "enum",
      defaultValue: '"bottom"',
      description: (
        <span>
          O lado preferido do gatilho para renderizar quando estiver aberto.
          Será invertido quando ocorrerem colisões e{" "}
          <code>avoidCollisions</code> estiver ativado.
        </span>
      ),
    },
    {
      name: "sideOffset",
      type: "number",
      defaultValue: "0",
      description: <span>A distância em pixels a partir do gatilho.</span>,
    },
    {
      name: "align",
      type: '"start" | "center" | "end"',
      typeSimple: "enum",
      defaultValue: '"center"',
      description: (
        <span>
          O alinhamento preferido em relação ao gatilho. Pode ser alterado
          quando ocorrem colisões.
        </span>
      ),
    },
    {
      name: "alignOffset",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          Um deslocamento em pixels a partir das opções de alinhamento{" "}
          <code>"start"</code> ou <code>"end"</code>.
        </span>
      ),
    },
    {
      name: "avoidCollisions",
      type: "boolean",
      defaultValue: "true",
      description: (
        <span>
          Quando <code>true</code>, substitui as preferências de{" "}
          <code>side</code> e <code>align</code> para evitar colisões com as
          bordas da fronteira.
        </span>
      ),
    },
    {
      name: "collisionBoundary",
      type: "Element | null | Array<Element | null>",
      typeSimple: "Boundary",
      defaultValue: "[]",
      description: (
        <span>
          O elemento usado como limite de colisão. Por padrão, isso é a janela
          de visualização (viewport), embora você possa fornecer elemento(s)
          adicional(is) a serem incluído(s) nesta verificação.
        </span>
      ),
    },
    {
      name: "collisionPadding",
      type: "number | Partial<Record<Side, number>>",
      typeSimple: "number | Padding",
      defaultValue: "0",
      description: (
        <span>
          A distância em pixels das bordas da fronteira onde a detecção de
          colisão deve ocorrer. Aceita um número (o mesmo para todos os lados)
          ou um objeto de preenchimento parcial, por exemplo: <code>{`{ top: 20, left: 20 }`}</code>
          .
        </span>
      ),
    },
    {
      name: "arrowPadding",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          O espaçamento entre a seta e as bordas do conteúdo. Se o seu conteúdo
          tiver <code>border-radius</code>, isso evitará que ele transborde
          pelos cantos.
        </span>
      ),
    },
    {
      name: "sticky",
      type: '"partial" | "always"',
      typeSimple: "enum",
      defaultValue: '"partial"',
      description: (
        <span>
          O comportamento de aderência no eixo de alinhamento.{" "}
          <code>"partial"</code> manterá o conteúdo dentro do limite enquanto o
          gatilho estiver pelo menos parcialmente dentro do limite, enquanto{" "}
          <code>"always"</code> manterá o conteúdo dentro do limite
          independentemente.
        </span>
      ),
    },
    {
      name: "hideWhenDetached",
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Se deve ocultar o conteúdo quando o gatilho ficar completamente
          oculto.
        </span>
      ),
    },
    {
      name: "condensed",
      required: false,
      typeSimple: "boolean",
      defaultValue: "true",
      description:
        "Tornar os itens mais condensados do que o comportamento comum.",
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
    {
      name: "[data-side]",
      value: ["left", "right", "bottom", "top"],
    },
    {
      name: "[data-align]",
      value: ["start", "end", "center"],
    },
  ]}
/>

### Item

O componente que contém os itens do menubar.

<MoreInfo
  componentName="Radix Menu Bar Item"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#item"
/>

<APIReference
  data={[
    {
      name: "disabled",
      type: "boolean",
      description: (
        <span>
          Quando <code>true</code>, impede que o usuário interaja com o item.
        </span>
      ),
    },
    {
      name: "onSelect",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o usuário seleciona um item (por meio do mouse
          ou teclado). Chamar <code>event.preventDefault</code> neste evento irá
          impedir que o menu seja fechado ao selecionar esse item.
        </span>
      ),
    },
    {
      name: "textValue",
      type: "string",
      description: (
        <span>
          Texto opcional usado para fins de digitação automática (typeahead).
          Por padrão, o comportamento de digitação automática usará o{" "}
          <code>.textContent</code> do item. Use isso quando o conteúdo for
          complexo ou quando houver conteúdo não textual dentro do item.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-highlighted]",
      value: "Presente quando destacado.",
    },
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### Group

Usado para agrupar vários `Menubar.Item`'s.

<MoreInfo
  componentName="Radix Menu Bar Group"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#group"
/>

### Label

Usado para renderizar um rótulo. Ele não será focável usando as teclas de seta.

<MoreInfo
  componentName="Radix Menu Bar Label"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#label"
/>

### CheckboxItem

Um item que pode ser controlado e renderizado como uma caixa de seleção.

<MoreInfo
  componentName="Radix Menu Bar Checkbox Item"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#checkboxitem"
/>

<APIReference
  data={[
    {
      name: "checked",
      type: `boolean | 'indeterminate'`,
      description: (
        <span>
          O estado controlado de verificação (checked) do item. Deve ser usado
          em conjunto com <code>onCheckedChange</code>.
        </span>
      ),
    },
    {
      name: "onCheckedChange",
      type: "(checked: boolean) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o estado de verificação (checked) muda.
        </span>
      ),
    },
    {
      name: "disabled",
      type: "boolean",
      description: (
        <span>
          Quando <code>true</code>, impede que o usuário interaja com o item.
        </span>
      ),
    },
    {
      name: "onSelect",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o usuário seleciona um item (via mouse ou
          teclado). Chamar <code>event.preventDefault</code> neste evento irá
          evitar que o menu seja fechado ao selecionar esse item.
        </span>
      ),
    },
    {
      name: "textValue",
      type: "string",
      description: (
        <span>
          Texto opcional usado para fins de digitação automática (typeahead).
          Por padrão, o comportamento de digitação automática usará o{" "}
          <code>.textContent</code> do item. Use isso quando o conteúdo for
          complexo ou quando houver conteúdo não textual dentro do item.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["checked", "unchecked"],
    },
    {
      name: "[data-highlighted]",
      value: "Presente quando destacado.",
    },
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### RadioGroup

Usado para agrupar vários `Menubar.RadioItem`'s.

<MoreInfo
  componentName="Radix Menu Bar Radio Group"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#radiogroup"
/>

<APIReference
  data={[
    {
      name: "value",
      type: "string",
      description: "O valor do item selecionado no grupo.",
    },
    {
      name: "onValueChange",
      type: "(value: string) => void",
      typeSimple: "function",
      description: "Evento chamado quando o valor muda.",
    },
  ]}
/>

### RadioItem

É uma mistura de [Radix Menu Radio Item](https://www.radix-ui.com/primitives/docs/components/menubar#radioitem) e [Radix Menu Bar Item Indicator](https://www.radix-ui.com/primitives/docs/components/menubar#itemindicator).<br/><br/>
Um item que pode ser controlado e renderizado como um rádio. É renderizado quando o elemento pai `Menubar.CheckboxItem` ou `Menubar.RadioItem` está marcado. Você pode estilizar este elemento diretamente ou usá-lo como um invólucro para inserir um ícone ou ambos.

<MoreInfo
  componentName="Radix Menu Bar Radio Item"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#radioitem"
/>

<MoreInfo
  componentName="Radix Menu Bar Item Indicator"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#itemindicator"
/>

<APIReference
  data={[
    {
      name: "value",
      type: "string",
      required: true,
      description: "O valor único do item.",
    },
    {
      name: "disabled",
      type: "boolean",
      description: (
        <span>
          Quando <code>true</code>, impede que o usuário interaja com o item.
        </span>
      ),
    },
    {
      name: "onSelect",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o usuário seleciona um item (por meio do mouse
          ou teclado). Chamar <code>event.preventDefault</code> neste evento
          impedirá que o menu seja fechado ao selecionar esse item.
        </span>
      ),
    },
    {
      name: "textValue",
      type: "string",
      description: (
        <span>
          Texto opcional usado para fins de digitação automática (typeahead).
          Por padrão, o comportamento de digitação automática usará o{" "}
          <code>.textContent</code> do item. Use isso quando o conteúdo for
          complexo ou quando houver conteúdo não textual dentro do item.
        </span>
      ),
    },
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Usado para forçar a montagem quando mais controle é necessário. Útil
          ao controlar animações com bibliotecas de animação React.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["checked", "unchecked"],
    },
    {
      name: "[data-highlighted]",
      value: "Presente quando destacado.",
    },
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### Separator

Usado para separar visualmente itens em um menu de barra de menu.

<MoreInfo
  componentName="Radix Menu Bar Separator"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#separator"
/>

### SubMenu

Contém todas as partes do submenu.

<MoreInfo
  componentName="Radix Menu Bar Sub Menu"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#sub"
/>

<APIReference
  data={[
    {
      name: "defaultOpen",
      type: "boolean",
      description: (
        <span>
          O estado de abertura do submenu quando ele é inicialmente renderizado.
          Use quando você não precisa controlar o estado de abertura dele.
        </span>
      ),
    },
    {
      name: "open",
      type: "boolean",
      description: (
        <span>
          O estado controlado de abertura do submenu. Deve ser usado em conjunto
          com <code>onOpenChange</code>.
        </span>
      ),
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      typeSimple: "function",
      description: (
        <span>Evento chamado quando o estado de abertura do submenu muda.</span>
      ),
    },
  ]}
/>

### SubMenuTrigger

Um item que abre um submenu. Deve ser renderizado dentro de `Menubar.Sub`.

<MoreInfo
  componentName="Radix Menu Bar Sub Menu Trigger"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#subtrigger"
/>

<APIReference
  data={[
    {
      name: "disabled",
      type: "boolean",
      description: (
        <span>
          Quando <code>true</code>, impede que o usuário interaja com o item.
        </span>
      ),
    },
    {
      name: "textValue",
      type: "string",
      description: (
        <span>
          Texto opcional usado para fins de digitação automática (typeahead).
          Por padrão, o comportamento de digitação automática usará o{" "}
          <code>.textContent</code> do item. Use isso quando o conteúdo for
          complexo ou quando houver conteúdo não textual dentro do item.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
    {
      name: "[data-highlighted]",
      value: "Presente quando destacado.",
    },
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### SubMenuContent

O componente que aparece quando um submenu está aberto. Deve ser renderizado dentro de `Menubar.Sub`.

<MoreInfo
  componentName="Radix Menu Bar Sub Menu Content"
  componentLink="https://www.radix-ui.com/primitives/docs/components/menubar#subcontent"
/>

<APIReference
  data={[
    {
      name: "loop",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Quando <code>true</code>, a navegação pelo teclado fará um loop do
          último item para o primeiro e vice-versa.
        </span>
      ),
    },
    {
      name: "onEscapeKeyDown",
      type: "(event: KeyboardEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando a tecla <code>Esc</code> está pressionada. Pode
          ser evitado chamando <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onPointerDownOutside",
      type: "(event: PointerDownOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando ocorre um evento de ponteiro fora dos limites do
          componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onFocusOutside",
      type: "(event: FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando o foco se move para fora dos limites do
          componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onInteractOutside",
      type: "(event: PointerDownOutsideEvent | FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Evento chamado quando ocorre uma interação (evento de ponteiro ou
          foco) fora dos limites do componente. Isso pode ser evitado chamando{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Usado para forçar a montagem quando mais controle é necessário. Útil
          ao controlar animações com bibliotecas de animação React. Ele herda de
          <code>Menubar.Portal</code>.
        </span>
      ),
    },
    {
      name: "sideOffset",
      type: "number",
      defaultValue: "6",
      description: <span>A distância em pixels a partir do gatilho.</span>,
    },
    {
      name: "alignOffset",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          Um deslocamento em pixels a partir das opções de alinhamento{" "}
          <code>"start"</code> ou <code>"end"</code>.
        </span>
      ),
    },
    {
      name: "avoidCollisions",
      type: "boolean",
      defaultValue: "true",
      description: (
        <span>
          Quando <code>true</code>, substitui as preferências de{" "}
          <code>side</code> e <code>align</code> para evitar colisões com as
          bordas da fronteira.
        </span>
      ),
    },
    {
      name: "collisionBoundary",
      type: "Element | null | Array<Element | null>",
      typeSimple: "Boundary",
      defaultValue: "[]",
      description: (
        <span>
          O elemento usado como limite de colisão. Por padrão, isso é a janela
          de visualização (viewport), embora você possa fornecer elemento(s)
          adicional(is) a serem incluído(s) nesta verificação.
        </span>
      ),
    },
    {
      name: "collisionPadding",
      type: "number | Partial<Record<Side, number>>",
      typeSimple: "number | Padding",
      defaultValue: "16",
      description: (
        <span>
          A distância em pixels das bordas da fronteira onde a detecção de
          colisão deve ocorrer. Aceita um número (o mesmo para todos os lados)
          ou um objeto de preenchimento parcial, por exemplo: <code>{`{ top: 20, left: 20 }`}</code>
          .
        </span>
      ),
    },
    {
      name: "arrowPadding",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          O espaçamento entre a seta e as bordas do conteúdo. Se o seu conteúdo
          tiver <code>border-radius</code>, isso evitará que ele transborde
          pelos cantos.
        </span>
      ),
    },
    {
      name: "sticky",
      type: '"partial" | "always"',
      typeSimple: "enum",
      defaultValue: '"partial"',
      description: (
        <span>
          O comportamento de aderência no eixo de alinhamento.{" "}
          <code>"partial"</code> manterá o conteúdo dentro do limite enquanto o
          gatilho estiver pelo menos parcialmente dentro do limite, enquanto{" "}
          <code>"always"</code> manterá o conteúdo dentro do limite
          independentemente.
        </span>
      ),
    },
    {
      name: "hideWhenDetached",
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Se deve ocultar o conteúdo quando o gatilho ficar completamente
          oculto.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
    {
      name: "[data-side]",
      value: ["left", "right", "bottom", "top"],
    },
    {
      name: "[data-align]",
      value: ["start", "end", "center"],
    },
    {
      name: "[data-orientation]",
      value: ["vertical", "horizontal"],
    },
  ]}
/>

<NavigationNextPrevious
  previous={{
    link: "/docs/components/label",
    label: "Label",
  }}
  next={{
    link: "/docs/components/navigation-menu",
    label: "Navigation Menu",
  }}
/>
