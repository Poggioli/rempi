---
title: Input
slug: input
---

# Input

<Description text="Captura a entrada do usuário." />

<SelectVersion />

<PreviewComponent name="input" />

## Instalação

Instale o componente a partir da linha de comando.

```bash
yarn add @rempi-ui/input
```

## Uso

Importe o <strong>Input</strong> e junte todas as partes do componente.

```jsx
import { Input } from "@rempi-ui/input";

() => (
  <Input.Label alignItems="flex-start" flexDirection="column">
    Nome
    <Input.Root placeholder="Digite seu nome" />
  </Input.Label>
);
```

## API reference

### Root

Este componente é baseado no elemento <code>input</code>.

<APIReference
  data={[
    {
      name: "inputSize",
      type: '"small" | "standard" | "large"',
      typeSimple: "enum",
      defaultValue: '"standard"',
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-disabled]",
      value: "Presente quando desabilitado.",
    },
  ]}
/>

### Label

Renderiza uma Label acessível associado aos controles.<br />
Rempi-UI <strong>Input.Label</strong> é baseado no <strong>Rempi-UI Label</strong>

<MoreInfo
  componentName="Rempi-UI Label"
  componentLink="/docs/components/label"
/>

<DataReference
  data={[
    {
      name: "data-disabled",
      value: "Presente quando desabilitado.",
    },
    {
      name: "data-invalid",
      value: "Presente quando inválido.",
    },
  ]}
/>

<NavigationNextPrevious
  previous={{
    link: "/docs/components/hover-card",
    label: "Hover Card",
  }}
  next={{
    link: "/docs/components/label",
    label: "Label",
  }}
/>

<EditThisPage url="components/pt-BR/input/1.0.1.mdx" />