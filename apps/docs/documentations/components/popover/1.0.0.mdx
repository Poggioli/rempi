---
title: Popover
slug: popover
radixUI: https://www.radix-ui.com/primitives/docs/components/popover
radixAPI: https://www.radix-ui.com/primitives/docs/components/popover#api-reference
---

# Popover

<Description text="Displays rich content in a portal, triggered by a button." />

<Flex style={{ gap: 8 }} alignItems="center">
  <RadixUI href="https://www.radix-ui.com/primitives/docs/components/popover" />
  <RadixAPI href="https://www.radix-ui.com/primitives/docs/components/popover#api-reference" />
  <SelectVersion />
</Flex>

<PreviewComponent name="popover" />

## Installation

Install the component from your command line.

```bash
yarn add @rempi-ui/popover
```

## Usage

Import the <strong>Popover</strong> and piece all composition components together.

```jsx
import { IconButton } from "@rempi-ui/button";
import { Flex } from "@rempi-ui/Flex";
import { Popover } from "@rempi-ui/popover";
import { Typography } from "@rempi-ui/typography";
import { SlidersHorizontal } from "lucide-react";

() => (
  <Popover.Root>
    <Popover.Trigger asChild>
      <IconButton
        aria-label="settings"
        variant="text"
        shape="rounded"
        color="primary"
      >
        <SlidersHorizontal size={16} />
      </IconButton>
    </Popover.Trigger>
    <Popover.Content>
      <Flex flexDirection="column">
        <Typography
          fontSize="sm"
          fontWeight="normal"
          color="low-contrast"
          lineHeight={2}
        >
          Lorem ipsum dolor sit amet, consectetur adipiscing elit. Phasellus ut
          vulputate elit, at efficitur tortor. Fusce at dui est. Morbi ac mauris
          vehicula, sagittis tortor eget, ultricies erat. Vivamus suscipit, arcu
          a lobortis eleifend, augue tellus laoreet ipsum, vitae cursus magna
          erat sed lorem. Etiam at augue pellentesque, sollicitudin metus.
        </Typography>
      </Flex>
      <Popover.CloseCross aria-label="Close" />
      <Popover.Arrow />
    </Popover.Content>
  </Popover.Root>
);
```

## About

<strong>Rempi-UI Popover</strong> component is built on top of [Radix Popover](https://www.radix-ui.com/primitives/docs/components/popover).

## API reference

### Root

Contains all the parts of a popover.

<MoreInfo
  componentName="Radix Popover Root"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#root"
/>

<APIReference
  data={[
    {
      name: "defaultOpen",
      type: "boolean",
      description: (
        <span>
          The open state of the popover when it is initially rendered. Use when
          you do not need to control its open state.
        </span>
      ),
    },
    {
      name: "open",
      type: "boolean",
      description: (
        <span>
          The controlled open state of the popover. Must be used in conjunction
          with <code>onOpenChange</code>.
        </span>
      ),
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when the open state of the popover changes.
        </span>
      ),
    },
    {
      name: "modal",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          The modality of the popover. When set to <code>true</code>,
          interaction with outside elements will be disabled and only popover
          content will be visible to screen readers.
        </span>
      ),
    },
  ]}
/>

### Trigger

The button that toggles the popover. By default, the `Popover.Content` will position itself against the trigger.

<MoreInfo
  componentName="Radix Popover Trigger"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#trigger"
/>

<APIReference
  data={[
    {
      name: "asChild",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <>
          Change the default rendered element for the one passed as a child,
          merging their props and behavior.
          <br />
          <br />
          Read our <a href="../guides/composition">Composition</a> guide for more
          details.
        </>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
  ]}
/>

### Content

It's a mix of [Radix-UI Popover Portal](https://www.radix-ui.com/primitives/docs/components/popover#portal) and [Radix-UI Popover Content](https://www.radix-ui.com/primitives/docs/components/popover#content).<br/><br/>
When used, portals the content part into the `body`.The component that pops out when the popover is open.

<MoreInfo
  componentName="Radix Popover Portal"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#portal"
/>

<MoreInfo
  componentName="Radix Popover Content"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#content"
/>

<APIReference
  data={[
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries. If used on this
          part, it will be inherited by <code>Popover.Content</code>.
        </span>
      ),
    },
    {
      name: "container",
      type: "HTMLElement",
      defaultValue: "document.body",
      description: "Specify a container element to portal the content into.",
    },
    {
      name: "onOpenAutoFocus",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when focus moves into the component after
          opening. It can be prevented by calling{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onCloseAutoFocus",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when focus moves to the trigger after closing. It
          can be prevented by calling <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onEscapeKeyDown",
      type: "(event: KeyboardEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when the escape key is down. It can be prevented
          by calling <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onPointerDownOutside",
      type: "(event: PointerDownOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when a pointer event occurs outside the bounds of
          the component. It can be prevented by calling{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onFocusOutside",
      type: "(event: FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when focus moves outside the bounds of the
          component. It can be prevented by calling{" "}
          <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "onInteractOutside",
      type: "(event: PointerDownOutsideEvent | FocusOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <span>
          Event handler called when an interaction (pointer or focus event)
          happens outside the bounds of the component. It can be prevented by
          calling <code>event.preventDefault</code>.
        </span>
      ),
    },
    {
      name: "forceMount",
      type: "boolean",
      description: (
        <span>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries. It inherits from{" "}
          <code>Popover.Portal</code>.
        </span>
      ),
    },
    {
      name: "side",
      type: '"top" | "right" | "bottom" | "left"',
      typeSimple: "enum",
      defaultValue: '"bottom"',
      description: (
        <span>
          The preferred side of the anchor to render against when open. Will be
          reversed when collisions occur and <code>avoidCollisions</code> is
          enabled.
        </span>
      ),
    },
    {
      name: "sideOffset",
      type: "number",
      defaultValue: "0",
      description: <span>The distance in pixels from the anchor.</span>,
    },
    {
      name: "align",
      type: '"start" | "center" | "end"',
      typeSimple: "enum",
      defaultValue: '"center"',
      description: (
        <span>
          The preferred alignment against the anchor. May change when collisions
          occur.
        </span>
      ),
    },
    {
      name: "alignOffset",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          An offset in pixels from the <code>"start"</code> or{" "}
          <code>"end"</code> alignment options.
        </span>
      ),
    },
    {
      name: "avoidCollisions",
      type: "boolean",
      defaultValue: "true",
      description: (
        <span>
          When <code>true</code>, overrides the <code>side</code> and
          <code>align</code> preferences to prevent collisions with boundary edges.
        </span>
      ),
    },
    {
      name: "collisionBoundary",
      type: "Element | null | Array<Element | null>",
      typeSimple: "Boundary",
      defaultValue: "[]",
      description: (
        <span>
          The element used as the collision boundary. By default this is the
          viewport, though you can provide additional element(s) to be included
          in this check.
        </span>
      ),
    },
    {
      name: "collisionPadding",
      type: "number | Partial<Record<Side, number>>",
      typeSimple: "number | Padding",
      defaultValue: "0",
      description: (
        <span>
          The distance in pixels from the boundary edges where collision
          detection should occur. Accepts a number (same for all sides), or a
          partial padding object, for example: <code>{`{ top: 20, left: 20 }`}</code>
          .
        </span>
      ),
    },
    {
      name: "arrowPadding",
      type: "number",
      defaultValue: "0",
      description: (
        <span>
          The padding between the arrow and the edges of the content. If your
          content has <code>border-radius</code>, this will prevent it from
          overflowing the corners.
        </span>
      ),
    },
    {
      name: "sticky",
      type: '"partial" | "always"',
      typeSimple: "enum",
      defaultValue: '"partial"',
      description: (
        <span>
          The sticky behavior on the align axis. <code>"partial"</code> will
          keep the content in the boundary as long as the trigger is at least
          partially in the boundary whilst <code>"always"</code> will keep the
          content in the boundary regardless.
        </span>
      ),
    },
    {
      name: "hideWhenDetached",
      type: "boolean",
      defaultValue: "false",
      description: (
        <span>
          Whether to hide the content when the trigger becomes fully occluded.
        </span>
      ),
    },
  ]}
/>

<DataReference
  data={[
    {
      name: "[data-state]",
      value: ["open", "closed"],
    },
    {
      name: "[data-side]",
      value: ["left", "right", "bottom", "top"],
    },
    {
      name: "[data-align]",
      value: ["start", "end", "center"],
    },
  ]}
/>

### Arrow

An optional arrow element to render alongside the popover. This can be used to help visually link the anchor with the `Popover.Content`. Must be rendered inside `Popover.Content`.

<MoreInfo
  componentName="Radix Popover Arrow"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#arrow"
/>

<APIReference
  data={[
    {
      name: "asChild",
      required: false,
      type: "boolean",
      defaultValue: "false",
      description: (
        <>
          Change the default rendered element for the one passed as a child,
          merging their props and behavior.
          <br />
          <br />
          Read our <a href="../guides/composition">Composition</a> guide for more
          details.
        </>
      ),
    },
    {
      name: "width",
      type: "number",
      defaultValue: 10,
      description: <span>The width of the arrow in pixels.</span>,
    },
    {
      name: "height",
      type: "number",
      defaultValue: 5,
      description: <span>The height of the arrow in pixels.</span>,
    },
  ]}
/>

### Close

The button that closes an open popover.

<MoreInfo
  componentName="Radix Popover Close"
  componentLink="https://www.radix-ui.com/primitives/docs/components/popover#close"
/>

### CloseCross

The button with X icon that closes the dialog.

<APIReference
  data={[
    {
      name: "aria-label",
      required: true,
      type: "string",
      description: "aria-label to be announced when screen reader is active",
    },
  ]}
/>

<NavigationNextPrevious
  previous={{
    link: "/docs/components/navigation-menu",
    label: "Navigation Menu",
  }}
  next={{
    link: "/docs/components/progress",
    label: "Progress",
  }}
/>
