---
title: Flex
slug: flex
---

# Flex

<Description text="Component for creating flex layouts." />

<SelectVersion />

<PreviewComponent name="flex" />

## Installation

Install the component from your command line.

```bash
yarn add @rempi-ui/flex
```

## Usage

Import the <strong>Flex</strong>.

```jsx
import { Flex } from "@rempi-ui/flex";
import { Typography } from "@rempi-ui/typography";

() => (
  <Flex alignItems="center" justifyContents="center">
    <Typography>Flex</Typography>
  </Flex>
);
```

## API reference

### Flex

This component is based on the `<div />` element.

<APIReference
  data={[
    {
      name: "flexDirection",
      required: false,
      type: '"column" | "column-reverse" | "inherit" | "initial" | "revert" | "row" | "row-reverse" | "unset"',
      typeSimple: "enum",
      defaultValue: '"row"',
    },
    {
      name: "flexWrap",
      required: false,
      type: '"inherit" | "initial" | "nowrap" | "revert" | "unset" | "wrap" | "wrap-reverse"',
      typeSimple: "enum",
      defaultValue: '"wrap"',
    },
    {
      name: "justifyContent",
      required: false,
      type: '"center" | "end" | "flex-end" | "flex-start" | "inherit" | "initial" | "left" | "normal" | "revert" | "right" | "space-around" | "space-between" | "space-evenly" | "start" | "stretch" | "unset"',
      typeSimple: "enum",
      defaultValue: '"flex-start"',
    },
    {
      name: "alignItems",
      required: false,
      type: '"baseline" | "center" | "end" | "flex-end" | "flex-start" | "inherit" | "initial" | "normal" | "revert" | "self-end" | "self-start" | "start" | "stretch" | "unset"',
      typeSimple: "enum",
      defaultValue: '"flex-start"',
    },
    {
      name: "alignContents",
      required: false,
      type: '"baseline" | "center" | "end" | "flex-end" | "flex-start" | "inherit" | "initial" | "normal" | "revert" | "space-around" | "space-between" | "space-evenly" | "start" | "stretch" | "unset"',
      typeSimple: "enum",
    },
    {
      name: "alignSelf",
      required: false,
      type: '"auto" | "baseline" | "center" | "end" | "flex-end" | "flex-start" | "inherit" | "initial" | "normal" | "revert" | "self-end" | "self-start" | "start" | "stretch" | "unset"',
      typeSimple: "enum",
    },
  ]}
/>

<NavigationNextPrevious
  previous={{
    link: "/docs/components/dropdown-menu",
    label: "Dropdown Menu",
  }}
  next={{
    link: "/docs/components/heading",
    label: "Heading",
  }}
/>

<EditThisPage url="components/en-US/flex/1.0.1.mdx" />
