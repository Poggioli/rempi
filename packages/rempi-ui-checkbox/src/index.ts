import {
  CheckboxRootProps,
  CheckboxLabelProps,
  CheckboxContainerProps,
  CheckboxRoot,
  CheckboxLabel,
  CheckboxContainer,
} from "./Checkbox";

export const Checkbox = {
  Check: CheckboxRoot,
  Container: CheckboxContainer,
  Label: CheckboxLabel,
};

export type { CheckboxRootProps, CheckboxLabelProps, CheckboxContainerProps };
