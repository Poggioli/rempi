import {
  CheckboxRootProps,
  CheckboxLabelProps,
  CheckboxRoot,
  CheckboxLabel,
} from "./Checkbox";

export const Checkbox = {
  Check: CheckboxRoot,
  Label: CheckboxLabel,
};

export type { CheckboxRootProps, CheckboxLabelProps };
